<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/todo_app_navigation"
    app:startDestination="@id/toDoListFragment">
    <fragment
        android:id="@+id/toDoListFragment"
        android:name="com.example.todoapp.ui.list_screen.ToDoListFragment"
        android:label="fragment_to_do_list"
        tools:layout="@layout/fragment_to_do_list">
        <action
            android:id="@+id/action_toDoListFragment_to_toDoItemEditFragment"
            app:destination="@id/toDoItemEditFragment"
            app:enterAnim="@android:anim/slide_in_left"
            app:exitAnim="@android:anim/slide_in_left"
            app:popEnterAnim="@android:anim/slide_in_left"
            app:popExitAnim="@android:anim/slide_out_right" />
        <action
            android:id="@+id/action_toDoListFragment_to_toDoItemEditComposeFragment"
            app:destination="@id/toDoItemEditComposeFragment" />
    </fragment>
    <fragment
        android:id="@+id/toDoItemEditFragment"
        android:name="com.example.todoapp.ui.edit_screen.ToDoItemEditFragment"
        android:label="fragment_to_do_item_edit"
        tools:layout="@layout/fragment_to_do_item_edit" >
        <argument
            android:name="itemId"
            app:argType="string"/>
    </fragment>
    <fragment
        android:id="@+id/toDoItemEditComposeFragment"
        android:name="com.example.todoapp.ui.edit_screen_compose.ToDoItemEditComposeFragment"
        android:label="ToDoItemEditComposeFragment" >
        <argument
            android:name="itemId"
            app:argType="string" />
    </fragment>
</navigation>